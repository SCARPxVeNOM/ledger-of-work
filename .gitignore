node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example

# spike + demo output — never commit results, receipts, or keys
out/
*.local.json
result*.json
result*.page.html
result*.screenshot.png
receipt*.json

# playwright
test-results/
playwright-report/
.playwright/

.DS_Store
Thumbs.db

# Local agent/editor tooling — not part of the product
.claude/
.mcp.json
SETUP.md

# Product spec and scratch assets — publish decision left to the author
idea*.md
resourcelinks.png
.data/
apps/verify-page/dist/
apps/verify-page/dist-types/

# The wallet connector bundle: 3.7MB of Hedera SDK and WalletConnect, rebuilt by
# `pnpm --filter @low/web build` and by Dockerfile.web. Committing it would put a
# generated blob in every diff and let the shipped bundle drift from the lockfile.
apps/web/public/connect.js
