{
  "name": "@low/verify-page",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The verifier as a static page: no backend, no account, reads the public mirror node.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && npx --yes serve dist -l 8405"
  },
  "dependencies": {
    "@low/protocol": "workspace:*",
    "@low/proof": "workspace:*",
    "@low/worker": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
